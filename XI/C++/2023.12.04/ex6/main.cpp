#include <iostream>
#include <cstream>

using namespace std;

int main()
{
    char s[256], sc[256];
    char *p;
    int n;
    cin >> n;
    for(int i=0;i<n;i++){
        cin.get(s,256);
        int len = strlen(s);
    }
    sc=strcpy(s)
    return 0;
}
